extends layout4

include mixins/_pagination

block content
  .businesses
    //- button.open-btn Open Live Coming Data
    //- div(id="businesses-wrapper")
    //- script(src="/socket.io/socket.io.js")
      //- .search__results
    
    //- form.search(action='/api/search' method='GET')
    .search
      input.search__input(type="text" placeholder="Business Reg, Business name..." name="search")
      span
        i.fas.fa-search
        //- button(type='submit')
    table.table
      thead
        tr
          th REGISTRATION NO
          th BUSINESS NAME
          th NATURE OF BUSINESS
          th STATE
          th DATE
          th ACTION
        tbody.search__results--table(id="myTable")
    //- <table class="table">
    //-         <thead>
    //-             <tr>
    //-                 <th> REGISTRATION NO </th>
    //-                 <th> BUSINESS NAME </th>
    //-                 <th> NATURE OF BUSINESS </th>
    //-                 <th> STATE </th>
    //-                 <th> DATE </th>
    //-             </tr>
    //-         </thead>
    
    table.table.hidden
      thead
        tr
          th REGISTRATION NO
          th BUSINESS NAME
          th NATURE OF BUSINESS
          th STATE
          th DATE
          th ACTION
      tbody(id="myTable")
        each business in businesses
          tr
            td
              =business.regNumber
            td
              =business.businessName
            td
              =business.natureOfBusiness
            td
              =business.state
            td
              =business.dateOfReg
            td
              a.link__button(href=`/admin/business/${business.businessName}/${business._id}` style='color:white') VIEW
    +pagination(page, pages, total)  
      